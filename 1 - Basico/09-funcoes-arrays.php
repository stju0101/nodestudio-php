<?php 

/*

* is_array($array) = verifica se uma variavel é uma rray
* in_array($valor, $array) = verifica se um determinado valor existe em agluma posição
* array_keys($array) = retorna um novo array com as chaves do array passado como parãmetro
* array_values($array) = retorna um novo array com os valores do array passado como parãmetro
* array_merge($array1, $array2) = agrega o conteudo dos dois arrays
* array_pop($array) = exclui a ultima posição do array
* array_shift($array) = exclui a primeira posição do array
* array_unshift($array) = exlcui a primeira posição do array
* array_push($array, "valor", "valor") = adiciona um ou mais elementos no final de um array
* array_combine($keys, $values) = mescla os dois arrays passados
* array_sum() = calcula a soma dos elementos de um array
* explode("/" "19/10/1996") = transforma string em array
* implode("-", $arr) = transforma array em string
*/





?>